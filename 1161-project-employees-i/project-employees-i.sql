# Write your MySQL query statement below
SELECT P.PROJECT_ID,IFNULL(ROUND(AVG(E.EXPERIENCE_YEARS),2),0) AS AVERAGE_YEARS FROM PROJECT AS P LEFT JOIN EMPLOYEE AS E ON P.EMPLOYEE_ID=E.EMPLOYEE_ID GROUP BY p.PROJECT_ID ORDER BY p.PROJECT_ID;